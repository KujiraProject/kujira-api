language: python
python:
  - "2.7"

before_install:
  - pip install -r requirements.txt
  - pip install pytest pytest-cov pylint
  - pip install coveralls

script: |
  pylint . \r &&
  py.test tests/

after_success:
    - coveralls

branches:
  only:
    - travis_test
